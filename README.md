# Dev Amplify
Dev Amplify is a tool that generates a step-by-step learning path for a requested technology.

## Usage
1. Run `python -m pytest` to run the tests.
2. Use the `DevAmplify` class to generate a learning path for a technology.
